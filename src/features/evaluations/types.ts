export interface JointRow {
  joint: string;
  range: string;
  notes: string;
}

export interface StrengthRow {
  joint: string;
  strength: string;
  notes: string;
}

export interface SpecialTestRow {
  name: string;
  result: string;
  notes: string;
}
